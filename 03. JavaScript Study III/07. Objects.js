// Assign an object to the user variable
var user = {"name": "Ken the Ninja", "age": 14};

// Print the value with the key "name" using a dot
console.log(user.name);

// Print the value with the key "age" using []
console.log(user["age"]);