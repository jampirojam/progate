// Complete the attack function
function attack(item) {
    if (item === "throwing stars") {
      return "It's super effective!";
    } 
    return "It's effective!";
  }
  
  // Call the attack function with "throwing stars" as the argument, then print the return value
  console.log(attack("throwing stars"));
  