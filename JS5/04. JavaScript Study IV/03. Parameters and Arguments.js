// Add item as the parameter
function attack(item) {
    // Print "Ken the Ninja used ___ to attack!"
    console.log("Ken the Ninja used "+ item +" to attack!");
    
  }
  
  // Call the attack method with "throwing stars" as the argument
  attack("throwing stars");
  